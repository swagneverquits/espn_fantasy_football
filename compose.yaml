services:
  scraper:
    build: .
    container_name: fantasy-football-scraper
    restart: unless-stopped
    stop_grace_period: 30s
    init: true
    command: python -m fantasy_football.cli scrape all --storage gcs
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    environment:
      TZ: America/New_York
      GCS_BUCKET: "${GCS_BUCKET:-}"
    volumes:
      - ./config/leagues.toml:/app/config/leagues.toml:ro
      - ./results:/app/results



